# ── Context optimization ──────────────────────────────────────────────
# Glob patterns auto-scale as new guides are added.
# Claude can still read any file on demand — these just prevent
# automatic inclusion during codebase exploration.

# Link registry files (URL metadata, 300-900+ lines each)
src/data/linkRegistry/*Links.ts

# Glossary term files (term definitions, 100-350 lines each)
src/data/glossaryTerms/*Terms.ts

# Guide data files — HTML content strings and interactive-component data.
# Single-file data modules:
src/data/*Data.ts
# Multi-file data directories (each guide's subdirectory):
src/data/*Data/

# Structural files that grow linearly with each guide.
# The root CLAUDE.md already contains the guide table and component list.
src/data/guideRegistry.ts
src/components/mdx/index.ts

# Reference docs — read on demand, not needed for most tasks.
docs/
